const path = require('path');

module.exports = {
    i18n: {
        defaultLocale: 'ru',
        locales: ['en', 'ru', 'es', 'pt'],
        localeDetection: false,
    },
    react: { useSuspense: false }
};
